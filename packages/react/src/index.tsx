import { styled } from "../styles";

export const Button = styled("button", {
  backgroundColor: "$ignite500",
  color: "$gray100",
  fontWeight: "$medium",
  borderRadius: "$md",
  border: 0,
  padding: "$4",
});
